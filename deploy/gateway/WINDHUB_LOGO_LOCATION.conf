# Include this location inside the windhub.online TLS server block.
location = /logo.ico {
    root /etc/nginx/tls/windhub;
    default_type image/x-icon;
    add_header Cache-Control "no-store" always;
    try_files $uri =404;
}
